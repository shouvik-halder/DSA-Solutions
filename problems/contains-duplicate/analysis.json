{
  "pattern": "Hash Table",
  "timeComplexity": "O(n)",
  "spaceComplexity": "O(n)",
  "explanation": "The solution uses a hash table to track the occurrences of each number in the input array. As it iterates through the array, it checks if the number already exists in the hash table, returning true if a duplicate is found, otherwise adding the number to the table.",
  "improvements": null,
  "relatedPatterns": [
    "Set",
    "Sorting"
  ]
}