{
  "pattern": "Monotonic Stack",
  "timeComplexity": "O(n)",
  "spaceComplexity": "O(n)",
  "explanation": "The solution uses a stack to simulate the collision of asteroids. Positive asteroids are pushed onto the stack, and when a negative asteroid is encountered, collisions are resolved based on their sizes until no further collisions occur or the stack is empty.",
  "improvements": "Consider using a List instead of a Stack for potentially better performance with dynamic resizing, or null if optimal",
  "relatedPatterns": [
    "Stack",
    "Simulation"
  ]
}